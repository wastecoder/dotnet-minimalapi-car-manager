version: '3.8'

services:
  mysql-car-manager:
    image: mysql:8.0
    container_name: mysql-car-manager
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: CarManagerDb
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql
